# Banco de dados (arquivo único SQLite, criado automaticamente).
DATABASE_URL="file:./dev.db"

# Gere um valor novo e aleatório para produção, por exemplo:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="troque-isso-em-producao"

# Domínio final do site (usado no SEO e nas meta tags Open Graph).
SITE_URL="http://localhost:3000"
